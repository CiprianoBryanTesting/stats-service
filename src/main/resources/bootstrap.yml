server:
  port: 8101

management:
  endpoints:
    web:
      exposure:
        include: "*"

feign:
  circuitbreaker:
    enabled: false

spring:
  sleuth:
    sampler:
      probability: 1.0

proxy:
  student-service:
    url: http://localhost:8001